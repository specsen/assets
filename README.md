# RHENUS Babylon.js Assets

Static asset repository for the RHENUS Zoho Creator training simulator.

GitHub Pages asset root after Pages is enabled from `main` / `/(root)`:

`https://specsen.github.io/assets`

Example asset:

`https://specsen.github.io/assets/models/vehicles/vehicle_curtainsider_v10.glb`

The repository is intentionally static. `.nojekyll` is included so binary assets are served unchanged.
